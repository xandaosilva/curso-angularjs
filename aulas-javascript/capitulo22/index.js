// Variables var, let, const

// Var ( depreciado ) global
var email = "amin@email.com";

// Let dentro de escopo
let sum = 4 + 6;

// Const, valor pode ser armazenado apenas uma vez
const expression = (email === "admin@email.com");

console.log(email);
console.log(sum);
console.log(expression);